#include <iostream>
using namespace std;

int main() {
    int count = 0;
    
    for (int i = 10; i <= 49; i++) {
        cout << i << " ";
        count++;
        if (count == 10) {
            cout << endl;
            count = 0;
        }
    }
    
    return 0;
}
