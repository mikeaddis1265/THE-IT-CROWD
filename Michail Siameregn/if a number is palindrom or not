int N;
cout<<"enter a number : ";
cin>>N;

int first=N;
while(first>=10)
{
    first/=10;
}
int last=N%10;
if (last==first)
cout<<N<<" : is a Palindrome number "<<endl;
else

cout<<N<<" : is not a Palindrome number "<<endl;
